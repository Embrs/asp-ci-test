services:
  postgres:
    image: postgres:16
    container_name: aspci_postgres
    environment:
      POSTGRES_USER: harrydev
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: mydb
    ports:
      - '15432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
